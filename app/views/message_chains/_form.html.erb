<%= form_with(model: message_chain) do |form| %>
  <% if message_chain.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(message_chain.errors.count, "error") %> prohibited this message_chain from being saved:</h2>

      <ul>
        <% message_chain.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
